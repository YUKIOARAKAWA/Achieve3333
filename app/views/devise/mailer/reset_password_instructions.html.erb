<p>ようこそ <%= @resource.email %>!</p>

<p>下記リンクをクリックし、パスワードの再設定を行ってください。</p>

<p><%= link_to 'パスワードの変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールにこころあたりがない場合、このメールは無視してください。</p>
<p>上記のリンクをクリックするまで、あなたのパスワードは変更されません。</p>
